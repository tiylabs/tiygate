//! Embedding cache implementation (Phase 4).
//! Placeholder for Phase 1-3.

/// Embedding cache — caches deterministic embedding results.
/// Full implementation in Phase 4.
pub struct EmbeddingCache;

impl Default for EmbeddingCache {
    fn default() -> Self {
        Self::new()
    }
}

impl EmbeddingCache {
    pub fn new() -> Self {
        Self
    }
}
