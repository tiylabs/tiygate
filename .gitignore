# Rust / Cargo
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# Build artifacts
*.pdb
*.dll
*.so
*.dylib

# IDE / Editor
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
.DS_Store
.fleet/
.zed/
.workbuddy/

# Local env & secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt
secrets/
config/local*.toml
config/local*.yaml
config/local*.yml

# Runtime / data
data/
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
logs/
*.log

# models.dev baseline snapshot — committed once as the embedded
# build-time baseline; subsequent updates are fetched at runtime.
# Use `git update-index --assume-unchanged` after fresh clones that
# update the snapshot locally.
crates/store/data/models_dev_api.json

# Coverage / profiling
coverage/
*.profraw
*.profdata
.tarpaulin-info/

# OS
Thumbs.db

# Tauri client
/src-tauri/binaries/
/src-tauri/gen/
